1c4     /* if this is not a variable name, parser shouldn't think this is 1, then discard c, then get 4. */
        /* should be captured by a digit. if something starts with a digit should be a digit (unless around quotes or comment) */
